#### What is an expression
  * An expression is a piece of code that produces a value
#### What does a linter does
  * It is a tool that checks for potential errors(mostly syntax(esp grammatical) errors)
#### What is function
  * Reusable piece of code that carries out a task 